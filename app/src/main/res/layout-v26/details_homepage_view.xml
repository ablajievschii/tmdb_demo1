<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="viewModel"
            type="com.example.android.tmdbdemo1.ui.details.MovieDetailsViewModel" />
    </data>
    <ImageView
        android:id="@+id/movieHome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_glyphicons_basic_351_link"
        android:contentDescription="@string/go_to_movie_home"
        android:background="?android:attr/selectableItemBackground"
        android:tooltipText="@string/visit_homepage"
        android:focusable="true"
        android:clickable="true"
        app:homepage="@{viewModel.homepage}" />
</layout>